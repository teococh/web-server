window.onload = main();
function main() {
    
    for (let i = 0; i < 25; i++) {
        var tablero = document.getElementById("tablero");
        // Elementos necesarios o casi
        let newDiv = document.createElement("div");
        tablero.appendChild(newDiv);
    }
}